<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Live - Your Orbit</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white h-screen flex flex-col overflow-hidden">
    <div class="absolute top-0 left-0 right-0 p-4 z-20 flex justify-between bg-gradient-to-b from-black/80 to-transparent">
        <button onclick="window.location.href='../index.html'" class="bg-black/40 p-2 rounded-full">✕</button>
        <div class="bg-red-600 px-3 py-1 rounded-md flex items-center space-x-2">
            <div class="w-2 h-2 bg-white rounded-full animate-ping"></div>
            <span class="text-[10px] font-black uppercase tracking-widest text-white">Live</span>
        </div>
    </div>

    <video id="webcam" autoplay playsinline class="w-full h-full object-cover"></video>

    <script>
        async function startCamera() {
            try {
                const stream = await navigator.mediaDevices.getUserMedia({ video: true, audio: true });
                document.getElementById('webcam').srcObject = stream;
            } catch (err) {
                alert("Please allow camera access in your browser settings to go live!");
            }
        }
        window.onload = startCamera;
    </script>
</body>
</html>
